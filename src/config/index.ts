const config = {
  server: {
    host: 'https://jsonplaceholder.typicode.com',
    api: {
      getAllImages: {
        uri: '/photos',
      },
    },
    errors: {},
  },
  references: {},
};

export default config;
