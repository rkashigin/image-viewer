const config = {
  server: {
    host: 'http://jsonplaceholder.typicode.com',
    api: {
      getAllImages: {
        uri: '/photos',
      },
    },
    errors: {},
  },
  references: {},
};

export default config;
